% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/clean.R
\name{drop_label_rows}
\alias{drop_label_rows}
\title{Drop label rows: rows carrying fewer than \code{min_cells} populated cells.}
\usage{
drop_label_rows(df, min_cells = 2L)
}
\arguments{
\item{df}{A data frame.}

\item{min_cells}{Minimum number of non-blank cells a row must have to be kept.}
}
\value{
\code{df} without its label rows.
}
\description{
\code{\link[=clean_table]{clean_table()}} deliberately keeps every row that is not \emph{fully} blank, because
deciding that a sparse row is not a record needs the schema. But two sparse-row
species are structural, not data, in almost every lab export: the single-cell
\strong{section divider} (\code{"Physical Tests"}, \code{"Total Metals"}) that separates analyte
blocks, and the trailing \strong{footnote banner}. Both carry one populated cell.
}
\details{
This is the generic form of the rule; a caller that knows its schema can pass a
different \code{min_cells}.
}
