% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/units.R
\name{convert_units}
\alias{convert_units}
\title{Convert mass-concentration or mass-fraction values between common units.}
\usage{
convert_units(value, from, to)
}
\arguments{
\item{value}{Numeric vector.}

\item{from}{Character vector of source units (recycled to \code{length(value)}).}

\item{to}{Single target unit.}
}
\value{
Numeric vector of converted values (\code{NA} where not convertible).
}
\description{
Handles two ladders independently: \strong{mass/volume} (g/L, mg/L, ug/L, ng/L)
and \strong{mass/mass} (g/kg, mg/kg, ug/kg, ng/kg, mg/g, ug/g, ng/g, the latter
being the tissue/sediment units). Conversion stays within a ladder --
mass/volume and mass/mass are not interconvertible without a density, so a
cross-ladder request returns \code{NA}. The micro prefix is accepted as either the
micro sign (U+00B5, \code{"\\u00b5g/L"}) or Greek small mu (U+03BC, common from
instrument exports); both fold to the same unit.
}
\details{
Returns the value unchanged when units already match (case/space-insensitive).
When a non-identity conversion cannot be resolved -- an unknown unit or a
cross-ladder pair -- the result is \code{NA} \emph{and a warning is emitted}, so an
unsupported unit class is distinguishable from a value that was simply
missing (both would otherwise be a bare \code{NA}).
}
