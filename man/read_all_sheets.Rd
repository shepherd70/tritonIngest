% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sheets.R
\name{read_all_sheets}
\alias{read_all_sheets}
\title{Read every worksheet of a workbook.}
\usage{
read_all_sheets(
  path,
  col_names = TRUE,
  col_types = NULL,
  include_hidden = TRUE,
  sheets = NULL,
  formula_policy = c("warn", "error", "allow")
)
}
\arguments{
\item{path}{Path to a workbook.}

\item{col_names, col_types}{Passed to \code{\link[=read_tabular]{read_tabular()}}.}

\item{include_hidden}{Read \code{hidden} / \code{veryHidden} sheets too (the default).
Set \code{FALSE} to read only what a user would see in Excel.}

\item{sheets}{Optional character vector of sheet names, or integer positions,
to restrict the read to.}

\item{formula_policy}{Formula handling policy passed to \code{\link[=read_tabular]{read_tabular()}}.}
}
\value{
A named list of tibbles, one per sheet read, in workbook order. Each
carries a \code{sheet_visibility} attribute.
}
\description{
The multi-sheet counterpart to \code{\link[=read_tabular]{read_tabular()}}, which reads exactly one sheet.
Sheets that fail to read (an empty sheet, a chart sheet) are reported and
skipped rather than aborting the whole workbook.
}
