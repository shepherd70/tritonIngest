% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/layout.R
\name{transpose_table}
\alias{transpose_table}
\title{Transpose an analyte-by-sample results matrix into a tidy long table.}
\usage{
transpose_table(
  df,
  header_rows,
  body_rows,
  label_cols,
  sample_cols,
  na_strings = c("-", "--", "n/a", "N/A")
)
}
\arguments{
\item{df}{A data frame read all-text with the header \strong{not} promoted
(\code{read_tabular(col_names = FALSE)}), so the sample-describing rows are data.}

\item{header_rows}{Named integer vector: output column name -> row index, e.g.
\code{c(location = 1, lab_id = 2, sample_date = 3, sample_time = 4)}.}

\item{body_rows}{Integer vector of rows holding analyte results.}

\item{label_cols}{Named integer vector: output column name -> column index for
the per-analyte label columns, e.g. \code{c(parameter = 1, units = 2)}.}

\item{sample_cols}{Integer vector of columns holding sample results.}

\item{na_strings}{Cell values dropped as "not measured".}
}
\value{
A long tibble: one row per (sample, analyte), with the \code{header_rows}
and \code{label_cols} names as columns plus \code{value_raw}.
}
\description{
Converts the shape \code{\link[=looks_transposed]{looks_transposed()}} detects. The first \code{n_label_cols}
columns describe each analyte (its name, its units, guideline columns, ...) and
the remaining columns are one per sample. \code{header_rows} names the rows above the
analyte block that describe each sample (sample id, date, time, location);
each becomes a column of the output.
}
