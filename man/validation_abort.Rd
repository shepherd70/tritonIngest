% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/validate.R
\name{validation_abort}
\alias{validation_abort}
\title{Abort with a classed error listing all collected validation failures}
\usage{
validation_abort(failures, class = "triton_validation_error", header = NULL)
}
\arguments{
\item{failures}{Character vector of failure messages (possibly empty).}

\item{class}{Additional condition class for the error, prepended to
\code{"triton_validation_error"} so callers can keep package-specific
classes (e.g. \code{"cpue_validation_error"}).}

\item{header}{Optional header line; defaults to a count of issues.}
}
\value{
Invisibly \code{TRUE} when \code{failures} is empty; otherwise signals an
error of class \code{c(class, "triton_validation_error")} whose
\code{failures} field holds the full message vector.
}
\description{
Standardizes the collect-all-failures-then-abort pattern: run every
check, concatenate the returned failure messages, and call this once.
Does nothing when \code{failures} is empty, so it can be called
unconditionally at the end of a validator.
}
\seealso{
Other validation:
\code{\link[=check_column_types]{check_column_types()}},
\code{\link[=check_monotonic]{check_monotonic()}},
\code{\link[=check_no_na]{check_no_na()}},
\code{\link[=check_range]{check_range()}},
\code{\link[=check_required_columns]{check_required_columns()}},
\code{\link[=check_unique]{check_unique()}},
\code{\link[=type_matches]{type_matches()}}
}
\concept{validation}
