% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sheets.R
\name{list_sheets}
\alias{list_sheets}
\title{List the worksheets of a workbook, with their visibility.}
\usage{
list_sheets(path)
}
\arguments{
\item{path}{Path to an \code{.xlsx}/\code{.xlsm}/\code{.xls} workbook.}
}
\value{
A tibble with \code{index} (1-based sheet position, as accepted by
\code{read_tabular(sheet = )}), \code{name}, and \code{visible} (\code{"visible"}, \code{"hidden"},
\code{"veryHidden"}, or \code{NA}).
}
\description{
\code{readxl::excel_sheets()} returns every sheet's name but not whether Excel
hides it: a \code{veryHidden} sheet in position 1 is what \code{read_tabular()} reads by
default, with no signal. This reads \code{xl/workbook.xml} directly to recover the
\code{state} attribute.
}
\details{
For a legacy \code{.xls} (OLE2) workbook, or any file whose \code{workbook.xml} cannot be
parsed, the names still come back but \code{visible} is \code{NA}.
}
