# Deterministic fingerprint of a contract

Deterministic fingerprint of a contract

## Usage

``` r
contract_fingerprint(contract)
```

## Arguments

- contract:

  Contract object.

## Value

SHA-256 hexadecimal fingerprint.
