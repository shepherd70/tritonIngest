# Field names of a contract.

Field names of a contract.

## Usage

``` r
contract_fields(contract)
```

## Arguments

- contract:

  A contract (list of specs or tibble).

## Value

Character vector of field names, in contract order.
